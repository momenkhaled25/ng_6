import { CardProductDirective } from './card-product.directive';

describe('CardProductDirective', () => {
  it('should create an instance', () => {
    const directive = new CardProductDirective();
    expect(directive).toBeTruthy();
  });
});
